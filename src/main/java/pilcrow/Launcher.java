package pilcrow;

import javafx.application.Application;

/**
 * The starting point into the Pilcrow program. Run to launch the GUI.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Gui.class, args);
    }
}