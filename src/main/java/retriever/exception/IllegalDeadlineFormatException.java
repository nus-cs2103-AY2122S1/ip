package retriever.exception;

public class IllegalDeadlineFormatException extends RetrieverException {
    public IllegalDeadlineFormatException(String errorMessage) {
        super(errorMessage);
    }
}
