package duke;

/**
 * Respective enum representation of types of commands that user has access to.
 */
public enum CommandType {
    Exit,
    List,
    MarkAsDone,
    DeleteTask,
    Find,
    Error,
    AddToDo,
    AddDeadline,
    AddEvent,
    Archive
}
