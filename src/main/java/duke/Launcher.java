package duke;

import javafx.application.Application;

/**
 * Implements a Launcher object that starts up the GUI.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Duke.class, args);
    }
}
