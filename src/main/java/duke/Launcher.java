package duke;

import javafx.application.Application;

/**
 * The class used as the entry point to launch Duke.
 */
public class Launcher {
    /**
     * Launches Duke.
     *
     * @param args
     */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
