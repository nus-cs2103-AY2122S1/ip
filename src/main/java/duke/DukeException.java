package duke;

/** The class which encapsulates exceptions raised specifically by Duke. */
public class DukeException extends Exception {

    /** Initializes the DukeException object with a message. */
    public DukeException(String message) {
        super(message);
    }
}
