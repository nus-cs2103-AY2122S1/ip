package duke;

import duke.ui.Main;
import javafx.application.Application;
public class DuCRunner {
    /**
     * Starts the DuC chat bot program
     * @param args argument for runner class
     */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
