package petal.components;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import stubs.FormatTest;
import stubs.StorageStub;
import stubs.TaskListStub;
import static org.junit.jupiter.api.Assertions.assertEquals;
import java.io.ByteArrayOutputStream;
import java.io.PrintStream;

public class ParserTest {

//    private final ByteArrayOutputStream outputStream = new ByteArrayOutputStream();
//    private final FormatTest formatTest = new FormatTest();
//    private final TaskListStub taskListStub = new TaskListStub();
//    private final StorageStub storageStub = new StorageStub(new TaskListStub());
//    private final Parser parser = new Parser(storageStub, taskListStub);
//
//    @BeforeEach
//    public void setUp() {
//        System.setOut(new PrintStream(outputStream));
//    }
//
//    @Test
//    public void parseInput_list_matchExpected() {
//        parser.handleInput("list");
//        assertEquals(formatTest.formatForTest("No items in list yet!"), outputStream.toString().trim());
//
//    }
//
//    @Test
//    public void parseInput_bye_matchExpected() {
//        parser.handleInput("bye");
//        assertEquals("You're leaving :( I hope you return soon!", outputStream.toString().trim());
//
//    }
//
//    @Test
//    public void parseInput_done_matchExpected() {
//        parser.handleInput("done");
//        assertEquals("This task was marked as done!", outputStream.toString().trim());
//    }

//    @Test
//    public void parseInput_delete_matchExpected() {
//        parser.handleInput("delete");
//        assertEquals("Okay, I have deleted this task", outputStream.toString().trim());
//    }
//
//    @Test
//    public void parseInput_todo_matchExpected() {
//        parser.handleInput("todo");
//        assertEquals("This task was marked as done!", outputStream.toString().trim());
//    }
//
//    @Test
//    public void parseInput_delete_matchExpected() {
//        parser.handleInput("delete");
//        assertEquals("This task was marked as done!", outputStream.toString().trim());
//    }
}
